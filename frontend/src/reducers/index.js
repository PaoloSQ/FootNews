import { combineReducers } from 'redux'
// import equipoSeccionReducer from './equipoSeccionReducer'

const rootReducer = combineReducers({
  // seccion: equipoSeccionReducer,
})

export default rootReducer
